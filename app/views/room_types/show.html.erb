<h2>Detalle del Tipo de habitaci&oacute;n</h2>

<% if notice %>
  <div class="alert alert-success">
    <button type="button" class="close" data-dismiss="alert">×</button>
    <p><%= notice %></p>
  </div>
<% end %>

<p>
  <strong><%= t('activerecord.attributes.room_type.denomination') %>:</strong>
  <%= @room_type.denomination %>
</p>

<p>
  <strong><%= t('activerecord.attributes.room_type.amount') %>:</strong>
  <%= @room_type.amount %>
</p>

<div class="form-actions">
  <%= link_to 'Editar', edit_room_type_path(@room_type), { class: 'btn btn-primary' } %>
  <%= link_to 'Atras', room_types_path, { class: 'btn' } %>
</div>