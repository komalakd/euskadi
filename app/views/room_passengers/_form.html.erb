<%= form_for(@room_passenger) do |f| %>
  <% if @room_passenger.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@room_passenger.errors.count, "error") %> prohibited this room_passenger from being saved:</h2>

      <ul>
      <% @room_passenger.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :reservation_room_id %><br>
    <%= f.number_field :reservation_room_id %>
  </div>
  <div class="field">
    <%= f.label :passenger_id %><br>
    <%= f.number_field :passenger_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
