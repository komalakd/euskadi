<%= form_for(@group_room) do |f| %>
  <% if @group_room.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@group_room.errors.count, "error") %> prohibited this group_room from being saved:</h2>

      <ul>
      <% @group_room.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :room_id %><br>
    <%= f.number_field :room_id %>
  </div>
  <div class="field">
    <%= f.label :group_id %><br>
    <%= f.number_field :group_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
