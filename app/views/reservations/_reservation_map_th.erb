<tr>
  <th></th>
  <% @dates.each do |date| %>
    <% th_class = (date == Date.today ) ? 'class=today_column' : '' %>
    <th <%= th_class %> ><%= l(date, format: :abbr_name).capitalize %><br /><%= l(date, format: :number_short) %></th>
  <% end %>
</tr>