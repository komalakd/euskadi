<h2>Detalle del Grupo</h2>

<% if notice %>
  <div class="alert alert-success">
    <button type="button" class="close" data-dismiss="alert">×</button>
    <p><%= notice %></p>
  </div>
<% end %>

<p>
  <b><%= t('activerecord.attributes.group.name') %>:</b>
  <%= @group.name %>
</p>

<p>
  <b><%= t('activerecord.attributes.group.amount') %>:</b>
  <%= @group.amount %>
</p>

<div class="form-actions">
  <%= link_to 'Editar', edit_group_path(@group), { class: 'btn btn-primary' } %>
  <%= link_to 'Atras', groups_path, { class: 'btn' } %>
</div>